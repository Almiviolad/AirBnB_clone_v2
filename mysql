CREATE TABLE place (
  id INT AUTO_INCREMENT PRIMARY KEY,
  -- Add other columns for place details
  name VARCHAR(255),
  address VARCHAR(255),
  city VARCHAR(100),
  -- Add more columns as needed
);
